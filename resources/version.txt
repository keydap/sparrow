package net

const SparrowVersion = `$version`
const aboutStr = `{
"name"     : "Sparrow Identity Server",
"version"  : "$version",
"revision" : "$revision",
"time"     : "$time",
"scim_base"  : "/v2",
"oauth_base" : "/oauth2"
}`